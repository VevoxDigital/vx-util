
/// <reference types=".." />

import vxEventEmitterImpl from './event'
import vxLogger from './logger'
import vxProcessMain from './process'
import vxProcessWrapper from './process/wrapper'

export const EventEmitter = vxEventEmitterImpl
export const Logger = vxLogger
export const ProcessMain = vxProcessMain
export const ProcessWrapper = vxProcessWrapper
